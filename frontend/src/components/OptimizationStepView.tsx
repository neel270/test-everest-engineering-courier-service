// Deprecated: replaced by CombinedStepsView. Keeping a stub to avoid import errors if referenced elsewhere.
import React from 'react';
export const OptimizationStepView: React.FC<{ steps: unknown[] }> = () => null;